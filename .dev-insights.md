# AgentGuard 经验可观测性日志

> AHE 模式：每 5 轮迭代提炼一次经验，事实 > 策略

## 最近经验摘要

### 迭代 50 经验 (2026-05-20)

**事实**: `cargo test` 输出的3个 "failures" 是 stale binary artifact。重编译后全部通过。
**根因**: `cargo clippy` 触发重编译后测试二进制更新，但第一次 `cargo test` 运行的是旧二进制。
**经验**: 遇到测试失败时，先 `cargo test -p <crate> -- <module>` 单独跑一次确认。不要盲目修改测试断言。

**事实**: workflows.rs 测试覆盖从 15→32，tokens.rs 从 13→21。
**方法**: 优先补序列化/反序列化测试（模型 roundtrip）、边界条件测试（delete-twice、empty name、special chars）、状态聚合测试（mixed status counts）。
**价值**: 这些测试捕获 API 契约变更（如 serde rename_all 改动），是回归测试的核心。

### 迭代 51-52 经验 (2026-05-20)

**事实**: 前一个开发循环添加了 Claims.iat 字段和 scheduler_interval_ms u32→u64 变更，但未更新测试代码。编译失败导致全 workspace 无法测试。
**根因**: 结构体字段变更后，测试中的手动构造代码（非 derive Default）未同步更新。
**经验**: 修改公共结构体后，`grep -r "StructName {"` 找到所有手动构造点并同步更新。

**事实**: context.rs 的 AddMessageRequest 缺少 Serialize derive，导致整个 test module 编译失败（18个测试无法运行）。
**根因**: 测试中使用了 `serde_json::to_string(&req)` 但 struct 只有 Deserialize。
**经验**: 如果测试中有序列化断言，struct 必须同时 derive Serialize + Deserialize。

**事实**: health.rs 的 test_deep_health_returns_system_info 在磁盘 95% 时失败。
**根因**: 测试断言 `status == "healthy"` 但系统实际处于不健康状态。
**经验**: 系统状态依赖型测试应验证"返回值合法"而非"返回特定值"。

**事实**: cargo clean 释放了 14.6GB，磁盘从 95% 降到 60%。
**根因**: debug build artifacts 累积占满系统盘。
**经验**: 每轮开发结束执行 `cargo clean` 释放空间。

## 变更清单记录

| 迭代 | 修改 | 证据 | 根因 | 预测 | 验证 |
|------|------|------|------|------|------|
| 50 | workflows.rs +17 tests | 4203 total, 0 fail | 补充序列化+边界测试 | 捕获API契约变更 | ✅ |
| 50 | tokens.rs +8 tests | 21 total tokens tests | 补充分状态计算+排序 | 捕获token计算回归 | ✅ |
| 52 | scheduler.rs +12 tests | 4416 total, 0 fail | 序列化roundtrip+边界case | 捕获scheduler API变更 | ✅ |
| 52 | config.rs fix Claims.iat | 编译修复 | 新增iat字段未同步测试 | 修复编译错误 | ✅ |
| 52 | context.rs +Serialize derive | 18 tests unblocked | 测试需要序列化 | 恢复context测试 | ✅ |
| 52 | health.rs fix flaky test | 系统依赖断言 | 磁盘满→unhealthy | 测试不再flaky | ✅ |

## 失败模式库

| 模式 | 根因 | 解决方案 | 验证状态 |
|------|------|---------|---------|
| stale test binary | cargo clippy触发重编译但test缓存未更新 | 单独重跑特定模块测试确认 | ✅ 迭代50验证 |
| struct字段变更未同步测试 | 手动构造代码不走derive | grep -r "StructName {" 找所有构造点 | ✅ 迭代52验证 |
| 系统依赖型测试flaky | 断言特定状态值 | 验证返回值合法性而非特定值 | ✅ 迭代52验证 |

## 可复用经验（事实 > 策略）

| 类型 | 内容 | 来源 | 迁移性 |
|------|------|------|--------|
| 测试策略 | 序列化roundtrip测试最能捕获serde配置变更 | 迭代50 | 高 |
| 调试技巧 | 测试失败先单独重跑，再怀疑代码 | 迭代50 | 高 |

## Quality Gate — 2026-05-20 21:13

| 指标 | 值 | 状态 |
|------|-----|------|
| Tests passed | 4386 | ✅ UP from 4203 (+183) |
| Tests failed | 0 | ✅ (health test fixed in working dir) |
| Clippy warnings | 0 | ✅ |
| Production unwrap() | 3342 | ⚠️ 累积风险 |
| Disk usage | 80% (30G/40G) | ⚠️ 接近阈值, target/ 12G |

### 变更清单（working dir）
- health.rs: test_deep_health 改为接受 healthy/unhealthy（修复 flaky）
- linux-automation: config_mgmt/docker_ops/provisioning/rbac 新增代码（其他 agent）
- 新增 +183 tests（主要来自 linux-automation 扩展）

### 教训
- test_deep_health_returns_system_info 在 CI 环境中 flaky（磁盘>80% 时返回 unhealthy），修复为接受两种状态
- 工作目录有其他 agent 的未提交修改，质量门检查需要同时检查 clean main 和 working dir
