[package]
name = "gdpr-compliance"
version = "0.1.0"
edition = "2021"
description = "GDPR and EU AI Act compliance utilities"
license = "MIT"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

[lib]
path = "src/lib.rs"
